import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.background',
  appName: 'sayaK-Family Finder',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },

};

export default config;
